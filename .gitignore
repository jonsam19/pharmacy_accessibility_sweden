# Environment variables (contains API keys and secrets)
.env
.env.local
.env.*.local

# Data directories
data/raw/*.rds           # Prepared data files (large)
data/raw/*.feather       # Feather format files (if any)
data/results/            # Analysis output files
!data/raw/.gitkeep       # Keep directory structure
!data/results/.gitkeep   # Keep directory structure

# Output files
outputs/                 # Generated plots and tables
!outputs/.gitkeep        # Keep directory structure

# R and RStudio
.Rproj.user
.Rhistory
.RData
.Ruserdata
*.Rproj

# Temporary files
*~
*.tmp
*.log
*.swp
.DS_Store
Thumbs.db

# IDE and editor files
.vscode/
.idea/
*.code-workspace

# Claude Code settings (keep local only)
.claude/settings.local.json